// @ts-check
import {expect} from 'chai';
import {errors} from '../../lib/errors.js';

describe('Unit > errors', function () {
	it('Hello, world', function () {
		expect(errors()).to.equal('Hello from errors');
	});
});
